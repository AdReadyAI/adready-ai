# Supabase
.branches
.temp

# dotenvx
.env.keys
.env.local
.env.*.local

# Local env vars for Supabase CLI (GOOGLE_AUTH_CLIENT_ID, etc.)
.env

functions/.env
snippets/

# Local-only benchmark harness + fixtures (not part of the deployed agents)
scripts/

# Deno lockfile (not tracked upstream on main)
deno.lock
