# ---------------------------------------------------------------------------
# Core worker / queue
# ---------------------------------------------------------------------------
DATABASE_URL=postgresql://postgres:postgres@host.docker.internal:54322/postgres
QUEUE_NAME=jobs
CHANNEL_NAME=new_job

# dev | prod — dev enables debugpy (see WORKER_DEBUGPY docs)
MODE=dev

# DEBUG | INFO | WARNING | ERROR | CRITICAL — controls log verbosity only
LOG_LEVEL=DEBUG

# ---------------------------------------------------------------------------
# OpenRouter (visual captioning / context task)
# ---------------------------------------------------------------------------
OPENROUTER_API_KEY=
OPENROUTER_VISION_MODEL=google/gemini-2.5-flash

# ---------------------------------------------------------------------------
# Supabase (Storage downloads + result writes)
# ---------------------------------------------------------------------------
SUPABASE_URL=http://host.docker.internal:54321
SUPABASE_SERVICE_ROLE_KEY=
INTERNAL_TRIGGER_SECRET=

# ---------------------------------------------------------------------------
# AssemblyAI (audio transcription)
# ---------------------------------------------------------------------------
ASSEMBLYAI_API_KEY=

# ---------------------------------------------------------------------------
# Roboflow (OWLv2 reference detection + optional hosted EasyOCR)
# ---------------------------------------------------------------------------
ROBOFLOW_API_KEY=
ROBOFLOW_OWLV2_URL=https://infer.roboflow.com/owlv2/infer
ROBOFLOW_WORKSPACE_ID=
ROBOFLOW_OCR_WORKFLOW_ID=
ROBOFLOW_OCR_TIMEOUT_SECONDS=

# OCR production routing and private representative-frame storage.
OCR_CANDIDATE_MODE=fixed_4fps
OCR_EVIDENCE_BUCKET=ocr-evidence
OCR_EVIDENCE_STORAGE_TIMEOUT_SECONDS=30

# Explicit opt-in; this test invokes the paid hosted OCR workflow.
RUN_ROBOFLOW_OCR_SMOKE=0

# ---------------------------------------------------------------------------
# Frame-sampling model weights
# ---------------------------------------------------------------------------
WARM_MODELS=true
EAST_MODEL_PATH=/app/assets/models/frozen_east_text_detection.pb
MOBILECLIP_MODEL_NAME=MobileCLIP2-S0
MOBILECLIP_PRETRAINED_TAG=dfndr2b
