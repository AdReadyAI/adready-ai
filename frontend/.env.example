# The frontend connects to the API exposed by `supabase start`.
VITE_SUPABASE_URL=http://127.0.0.1:54321

# Copy the local ANON_KEY printed by `supabase status`.
VITE_SUPABASE_ANON_KEY=replace-with-local-anon-key
