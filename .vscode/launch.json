{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to worker container",
      "type": "debugpy",
      "request": "attach",
      "connect": { "host": "localhost", "port": 5678 },
      "pathMappings": [
        { "localRoot": "${workspaceFolder}/worker", "remoteRoot": "/app" }
      ]
    }
  ]
}
